<androidx.constraintlayout.widget.ConstraintLayout xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/films_id"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    android:background="@color/fragmentBackground">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/list_films"
        android:layout_width="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_height="0dp" />

    <ProgressBar
        android:id="@+id/films_progress"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/list_films"
        app:layout_constraintEnd_toEndOf="@+id/list_films"
        app:layout_constraintStart_toStartOf="@+id/list_films"
        app:layout_constraintTop_toTopOf="@+id/list_films"
        tools:visibility="visible" />
</androidx.constraintlayout.widget.ConstraintLayout>

